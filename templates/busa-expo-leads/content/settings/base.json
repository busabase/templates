{
  "name": "Settings",
  "description": "SLA thresholds, default reply templates, and the channel accounts used to send follow-ups, one row",
  "agentPrompts": [
    {
      "key": "adjust-sla",
      "label": "Adjust SLA thresholds or reply templates",
      "body": "{target}\n\nRead the `busa-expo-leads` skill in this folder and follow its workflow, then update the `sla-24h-hours` / `sla-72h-hours` thresholds or the per-language reply templates I describe on the settings record. Never write a secret value here — only the non-secret env var name that names the sending account."
    },
    {
      "key": "check-settings",
      "label": "What are my current SLA thresholds and sending accounts?",
      "intent": "read-only",
      "body": "{target}\n\nRead the `busa-expo-leads` skill in this folder, then show me the current SLA thresholds, reply templates, and the configured WhatsApp/email account env var names (never secret values)."
    }
  ],
  "position": 3,
  "fields": [
    {
      "slug": "record-id",
      "name": "Record ID",
      "type": "text",
      "required": true,
      "position": 0,
      "options": {}
    },
    {
      "slug": "sla-24h-hours",
      "name": "SLA 24h hours",
      "type": "number",
      "required": false,
      "position": 1,
      "options": {}
    },
    {
      "slug": "sla-72h-hours",
      "name": "SLA 72h hours",
      "type": "number",
      "required": false,
      "position": 2,
      "options": {}
    },
    {
      "slug": "reply-templates",
      "name": "Reply templates",
      "type": "longtext",
      "required": false,
      "position": 3,
      "options": {}
    },
    {
      "slug": "whatsapp-account-env",
      "name": "WhatsApp account env",
      "type": "text",
      "required": false,
      "position": 4,
      "options": {}
    },
    {
      "slug": "email-account-env",
      "name": "Email account env",
      "type": "text",
      "required": false,
      "position": 5,
      "options": {}
    }
  ],
  "views": []
}
