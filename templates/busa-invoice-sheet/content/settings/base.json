{
  "name": "Settings",
  "description": "Sanitized config summary (default currency, extraction preferences, review policy, export preferences — no secrets), one row keyed by kind",
  "position": 1,
  "fields": [
    {
      "slug": "record-id",
      "name": "Record ID",
      "type": "text",
      "required": true,
      "position": 0,
      "options": {}
    },
    {
      "slug": "kind",
      "name": "Kind",
      "type": "text",
      "required": true,
      "position": 1,
      "options": {}
    },
    {
      "slug": "payload",
      "name": "Payload (JSON object)",
      "type": "longtext",
      "required": false,
      "position": 2,
      "options": {}
    },
    {
      "slug": "updated-at",
      "name": "Updated at",
      "type": "text",
      "required": false,
      "position": 3,
      "options": {}
    }
  ],
  "views": [],
  "agentPrompts": [
    {
      "key": "check-extraction-defaults",
      "label": "Check extraction and currency defaults",
      "intent": "read-only",
      "body": "{target}\n\nRead the `busa-invoice-sheet` skill in this folder, then show the default currency and extraction preferences from the sanitized config summary. Never reveal or ask for secret values."
    },
    {
      "key": "check-review-export-policy",
      "label": "Check review and export policy",
      "intent": "read-only",
      "body": "{target}\n\nRead the `busa-invoice-sheet` skill in this folder, then show the current review policy and export preferences — what triggers `needs_review` vs `blocked`, and where approved exports land."
    }
  ]
}
