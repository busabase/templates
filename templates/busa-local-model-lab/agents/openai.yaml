interface:
  display_name: "Busa Local Model Lab"
  short_description: "Curate Busabase datasets and run reproducible MLX fine-tunes"
  default_prompt: "Use $busa-local-model-lab to review training data, run a local MLX fine-tune, and compare the adapter with its baseline."
